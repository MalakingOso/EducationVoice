# Virtual environments
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.Python
build/
dist/
*.manifest
*.spec

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Project outputs — generated audio and the scratch script from each run.
# Saved scripts worth keeping live in scripts/ and ARE tracked; the audio
# never is (large, regenerable from the script in ~11 min).
output/
*.wav
*.mp3
*.flac
*.ogg
!scripts/*.txt

# Downloaded VibeVoice preset voice clips (contents already match *.wav;
# this entry records the intent)
voices/

# Local / scratch
_ul
*.log
.env
.env.*
!.env.example

# Model / data caches
*.ckpt
*.pt
*.pth
*.safetensors
.cache/
